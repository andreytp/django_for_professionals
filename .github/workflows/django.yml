xname: Django CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build the stack
        run: |
          cd ./books
          docker-compose up -d
      - name: Test
        run: |
          cd ./books
          docker exec -it books_web_1 ls
#         docker-compose -f docker-compose.yml ps
#         docker-compose -f docker-compose.yml exec web  python manage.py test 2>&1 | echo
#         docker-compose -f docker-compose.yml exec web python manage.py test -v 3
          
#         run: docker-compose exec -d web python manage.py test -v 4

